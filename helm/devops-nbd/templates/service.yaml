apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-nbd.fullname" . }}
  labels:
    {{- include "devops-nbd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-nbd.selectorLabels" . | nindent 4 }}
