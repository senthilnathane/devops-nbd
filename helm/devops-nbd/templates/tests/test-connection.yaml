apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-nbd.fullname" . }}-test-connection"
  labels:
    {{- include "devops-nbd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-nbd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
